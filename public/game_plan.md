dont need the stream to be present in the top center of the screen and in the chat window (currently shown in two locations), only retain the one in the chat window and remove the top of screen one.

It hangs on reranking process for a long time then finally switches to say generating with webgpu and then fails to the object already been disposed message

cross encoder doesnt begin to load model weights until the first inquiry reaches that step in the pipeline when it goes to execute the reranker and then stops the process to load the model weights, after which you cannot get it to resume the search. It still only will maybe let you perform one search then the second you received the included error messages:

System error: Error: Object has already been disposed

System error: ModelNotLoadedError: Model not loaded before trying to complete ChatCompletionRequest. Please ensure you have called MLCEngine.reload(model) to load the model before initiating APIs, or initialize your engine using CreateMLCEngine() with a valid model configuration.

Im confused about what model and method it is programmed to use by defualt after launching and what it is loading from the cache. There is a drop down which defaults 
to SNOWflake_v1.2_UNCUTstash and offers a 1B and 3B version as well as many different options of Llama-3.2-Instruct but then also has a developer override which allows you to select from four versions of Llama-3.2 - 1B or 3B in either q4f16_1 or q4f32_1 with one drop down for primary target engine and another for speculative draft engine with the option to turn the draft tandem system off so that you can just select one model although none of these capabilities seemed to have worked. 

Then independent of these two drop downs there is a button that alternates between EDGE NETWORK which is the default or SOVEREIGN LOCAL but it doesnt indicate which of these two drop downs which seem to be conflicting, is for local use and which is for network use by accessing hugging face for the MLC models they have available which is what it is supposed to do.  I need to have a clear way to shutdown all network activity and use a local only model. For testing purposes, it would be ideal to boot into local mode with llama 3.2 1B. I also want to be able to access the entire hugging face catalog of MLC models available for use which needs to be able to get updated in real time whenever they add a new model.

i would assume it is booting into local mode because SNOWflake is selected by default but it shows loading Llama-3.2-1B-instruct on the loading screen which is the one i would like it to load locally without a drafting model when it initiates for development purposes and proof of concept, but at the top of the screen by default the mode is displaying as "EDGE NETWORK" so i am confused. 

The entire left sidebar is nonfunctional, so is the gear icon for settings, nothing happens when clicking any of these. It needs to actually log each chat instance into the recent sessions as intended, needs to have a working settings menu with the baseline parameters available in a standard chatgpt interface, needs to let you view and modify the OPFS RAG Explorer, and needs to allow me to at least open the P2P utility to see how it looks within the interface.

There needs to be a way i can refresh the page without it being triggered to load the model from scratch, it should be able to retain the model within the cache but still reload the other aspects of the application and if i want to reload the model i should have an option from the interface to specifically indicate if i want to reload the page but not reload the model, an option to reload the model from local OPFS storage back into the cache, and a third option to start over completely and trigger it to redownload the model into the local OPFS, which will then load itself from OPFS local storage ointo the browser cache upon successful completion of the download which means there are 3 different boot scenarios and should have the toggle for whther to boot in local or edge mode as well, defaulting to local mode (after downloading the model or if the model already exists in the OPFS) and upon switching to edge mode, the hugging face MLC models should then become available. The conflicting drop downs need to be corrected.

The toggleing between the ten backgrounds does not work. The only background is one which is not bad although it is more reminiscent of travelling thru the sky on santas sleigh on a snowy night than stars in a galaxy. It remains on this one and only option when switching throu the supposed ten choices.

The chat window which has a separate dropdown for selecting between five glassmorphic options is also nonfunctional and the chat window is invisible no matter which choice you select. Also, the sidebar is solid black when it should also be glassmorphic and needs to match the chat window in such a way that it can be distringuished from the main window but it cohesive and maintains the same style and level of transparency as the main chat window and must also change simultaneously with the main chat window when toggling between each of the five glassmorphic presets to provide a cohesive UX.